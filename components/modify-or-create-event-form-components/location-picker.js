"use client";

import React, { useState } from "react";
import AddJustAddress from "./addJustAddress";
import PickVenueInput from "./pick-venue-input";

export default function LocationPicker({ modifyEventOrEvent }) {
  const [locationType, setLocationType] = useState("venue"); // Default to venue

  return (
    <div className="w-full flex flex-col gap-4 mt-4">
     
    </div>
  );
}